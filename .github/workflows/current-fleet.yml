name: current-fleet

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  current-fleet:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - id: pins
        run: node scripts/current-fleet-pins.js >> "$GITHUB_OUTPUT"
      - uses: actions/checkout@v4
        with:
          repository: mike-axiom-mir/axm-morphtile-machine-form
          ref: ${{ steps.pins.outputs.form }}
          path: .siblings/current/form
      - uses: actions/checkout@v4
        with:
          repository: mike-axiom-mir/axm-morphtile-machine-surface
          ref: ${{ steps.pins.outputs.surface }}
          path: .siblings/current/surface
      - uses: actions/checkout@v4
        with:
          repository: mike-axiom-mir/axm-morphtile-machine-capability
          ref: ${{ steps.pins.outputs.capability }}
          path: .siblings/current/capability
      - uses: actions/checkout@v4
        with:
          repository: mike-axiom-mir/axm-morphtile-machine-interface
          ref: ${{ steps.pins.outputs.interface }}
          path: .siblings/current/interface
      - uses: actions/checkout@v4
        with:
          repository: mike-axiom-mir/axm-morphtile
          ref: ${{ steps.pins.outputs.core }}
          path: .runtime/morphtile
      - run: npm test
        env:
          CURRENT_FORM_MACHINE_PATH: ${{ github.workspace }}/.siblings/current/form/src
          CURRENT_FORM_COMMIT: ${{ steps.pins.outputs.form }}
          CURRENT_SURFACE_MACHINE_PATH: ${{ github.workspace }}/.siblings/current/surface/src
          CURRENT_SURFACE_COMMIT: ${{ steps.pins.outputs.surface }}
          CURRENT_CAPABILITY_MACHINE_PATH: ${{ github.workspace }}/.siblings/current/capability/src
          CURRENT_CAPABILITY_COMMIT: ${{ steps.pins.outputs.capability }}
          CURRENT_INTERFACE_MACHINE_PATH: ${{ github.workspace }}/.siblings/current/interface/src
          CURRENT_INTERFACE_COMMIT: ${{ steps.pins.outputs.interface }}
          CURRENT_MORPHTILE_CORE_PATH: ${{ github.workspace }}/.runtime/morphtile/core/morphtile.js
          CURRENT_MORPHTILE_COMMIT: ${{ steps.pins.outputs.core }}
